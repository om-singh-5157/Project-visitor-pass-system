<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container-fluid px-4">
    <a class="navbar-brand fw-bold" href="/">
      <i class="fas fa-building me-2"></i>Visitor System
    </a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarContent"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav ms-auto align-items-center">
        <% if (!currUser) { %>
        <li class="nav-item">
          <a class="nav-link" href="/admin/dashboard">
            <i class="fas fa-user-shield me-1"></i>Admin Panel
          </a>
        </li>
        <% } else { %>

        <!-- Optional Back to Dashboard (for user role only) -->
        <% if (typeof showBackButton !== 'undefined' && showBackButton &&
        currUser.role === 'user') { %>
        <li class="nav-item">
          <a class="nav-link" href="/visitor/<%= currUser._id %>/dashboard">
            <i class="fas fa-arrow-left me-1"></i>Dashboard
          </a>
        </li>
        <% } %>

        <!-- User Greeting -->
        <li class="nav-item">
          <span class="nav-link disabled">
            <i class="fas fa-user-circle me-1"></i>Hello, <%= currUser.username
            %>
          </span>
        </li>

        <!-- Logout -->
        <li class="nav-item">
          <a class="nav-link" href="/logout">
            <i class="fas fa-sign-out-alt me-1"></i>Logout
          </a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
